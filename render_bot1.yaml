services:
  - type: web
    name: crypto-5m-confirmation-bot-1
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 -b 0.0.0.0:$PORT main_crypto_confirmation_bot:app
    healthCheckPath: /health
